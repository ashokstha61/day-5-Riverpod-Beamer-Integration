// import 'package:flutter/material.dart';

// class AppColors {
//   static const primary = Color(0xFF6750A4);
//   static const secondary = Color(0xFF625B71);
//   static const background = Color(0xFFF5F5F5);
//   static const surface = Colors.white;
//   static const error = Colors.redAccent;
// }

import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const primary = Color(0xFF6750A4);
  static const secondary = Color(0xFF625B71);
  static const background = Color(0xFFF5F5F5);
  static const surface = Colors.white;
  static const error = Colors.redAccent;

  // Colors from HomePage
  static const deepPurple = Colors.deepPurple;
  static const teal = Colors.teal;
  static const orange = Colors.orange;
  static const pink = Colors.pink;
  static const blue = Colors.blue;
  static const green = Colors.green;
  
  // Text colors
  static const textPrimary = Color(0xDE000000); // Colors.black87
  static const textSecondary = Colors.grey;
  
  // Background shades
  static const greyBackground = Color(0xFFFAFAFA); // Colors.grey[50]
  static const white = Colors.white;
  
  // Shadow colors
  static Color shadowLight = Colors.black.withOpacity(0.05);
  static Color shadowMedium = Colors.black.withOpacity(0.3);
}